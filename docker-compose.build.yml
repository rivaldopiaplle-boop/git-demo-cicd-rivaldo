services:
  frontend:
    image: nginx:1.27-alpine
    ports:
      - "8080:80"
    restart: unless-stopped

  backend:
    image: alpine:3.20
    command: ["sh", "-c", "sleep infinity"]
    restart: unless-stopped
